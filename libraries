// get length

var name = "Tahmeed";
var nameLength = name.length;

// display 3rd (3rd == index 2) charecter

var name = "Tahmeed";
var sChar = name.charAt("2");

// join two string with libraries

var firstName = "Tahmeedul";
var lastName = " Islam";
var fName = firstName.concat(lastName);

// get any perticuler char

var fName = "Tahmeed";
var p1 = fName.slice(0, 1); // to get T
var p2 = fName.slice(3, 4); // to get m
var p3 = fName.slice(6, 7); // to get d
