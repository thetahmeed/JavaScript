// creating a function

function doSquare(num) // num is a parameter here that you have to pass when you are calling this funstion
{
    var result = num * num;
    
    return result; // here we are returning the value
}

// calling the function
document.write("Square = "+doSquare(4));
